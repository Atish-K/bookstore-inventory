// used by `ng build` (production)
// set apiUrl to wherever the backend is deployed, and add the frontend url to CORS_ORIGIN in the backend .env
export const environment = {
  production: true,
  apiUrl: 'https://your-backend-url.com/api'
};
